- name: Install nginx
  hosts: all
  become: true
  tasks:
    - name: Install nginx
      ansible.builtin.yum:
        name: nginx
        state: latest

    - name: Starting nginx
      ansible.builtin.systemd:
        name: nginx
        state: started
        enabled: true