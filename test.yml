- name: Testing the Variables
  hosts: localhost
  connection: local
  vars:
    name: "Nageswara Rao"

    tasks:
      - name: Printing the Variable
        ansible.builtin.debug:
          msg:  The Name is  {{ name }}

