- name: Testing the Variables
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    name: "Nageswara Rao"

    tasks:
      - name: Printing the Variable
        ansible.builtin.debug:
          msg:  The Name is  {{ name }}

