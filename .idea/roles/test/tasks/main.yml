name: Install nginx
hosts: all
tasks:
  - name: Install nginx
    ansible.builtin.yum:
      name: nginx
      state: latest

  - name: Starting nginx
    ansible.builtin.systemd:
      name: nginx
      state: started
      enabled: true